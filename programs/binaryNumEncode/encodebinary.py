#!/bin/python
import argparse, sys, os, time
parser = argparse.ArgumentParser(description ='Decimal To Binary') 
parser.
val=0
def stepper(val):
    out=val*2
    return out
def digitEq(in1):
    inputNum=int(in1)
    out=1
    countA=0
    participants[0]=1
    while True:
        stepper(out)

